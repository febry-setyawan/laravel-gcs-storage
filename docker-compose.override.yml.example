# Docker Compose Override Template
# Copy this file to docker-compose.override.yml and customize as needed
# This file is ignored by git for local development customization

services:
    app:
        # Uncomment to enable Xdebug
        # environment:
        #     XDEBUG_MODE: develop,debug
        #     XDEBUG_CONFIG: client_host=host.docker.internal
        
        # Uncomment to customize ports
        # ports:
        #     - '8001:80'
    
    mysql:
        # Uncomment to customize MySQL port
        # ports:
        #     - '3307:3306'
        
        # Uncomment to use a different MySQL version
        # image: 'mysql/mysql-server:5.7'
    
    redis:
        # Uncomment to customize Redis port  
        # ports:
        #     - '6380:6379'
    
    phpmyadmin:
        # Uncomment to customize phpMyAdmin port
        # ports:
        #     - '8081:80'
    
    # Uncomment to add MailHog for email testing
    # mailhog:
    #     image: mailhog/mailhog:latest
    #     ports:
    #         - '1025:1025'
    #         - '8025:8025'
    #     networks:
    #         - laravel-gcs